---
- hosts: all
  gather_facts: true
  become: yes
  
  roles:
    - { role: geerlingguy.repo-epel, when: '"control" in inventory_hostname' }
    - { role: geerlingguy.nodejs, when: '"control" in inventory_hostname' }
    - { role: wetty, when: '"control" in inventory_hostname' }
